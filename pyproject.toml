[tool.poetry]
name = "protopy"
version = "0.0.1"
description = "Python Library to generate protobuffers code from python using type hints"
authors = ["Pablo España <pabloespanab@outlook.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
